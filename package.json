{
  "name": "playmori-kids-growth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/server.mjs",
    "dev": "node --watch server/server.mjs",
    "test": "node --test",
    "check": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
