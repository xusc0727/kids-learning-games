# 项目协作规则

## 个人知识库同步

- 本项目的任何修改，包括但不限于代码、配置、数据库、部署流程、发布分支、产品规则和文档变更，都必须在任务完成前同步更新到个人知识库中的相关文档。
- 个人知识库绝对路径：`/Users/xushichao/Library/Mobile Documents/iCloud~md~obsidian/Documents/xusc`。
- 同步前先读取知识库内的 `AGENTS.md`、目录结构、目标目录中的现有笔记，并遵循其 PARA 分类、命名、frontmatter、标签和内部链接规范。
- 优先更新已有的项目相关笔记；没有合适笔记时，按知识库规范创建新笔记，并记录本次修改内容、影响范围、验证结果及必要的发布或回滚说明。
- 完成项目修改但尚未完成知识库同步时，不得将任务标记为完成；如因权限或外部状态无法同步，必须明确告知用户具体阻塞原因和待同步内容。
- 向用户交付时，同时报告项目内修改文件和知识库最终更新文件的绝对路径。

## `prd` 版本发布文档

- 每次执行会让 `prd` 增加代码的合并、快进、挑选提交或直接提交前，必须比较 `prd` 与上一次发布的差异，并为本次发布单独整理一份版本功能文档。
- 版本文档统一存放在个人知识库项目目录的 `05-版本发布/` 下；一个版本对应一个独立 Markdown 文件，禁止覆盖或复用旧版本文档。
- 文件名使用 `YYYY-MM-DD 版本主题.md`；同一天多次发布时，在日期后增加明确的序号或版本号。
- 每份版本文档至少记录：发布时间与分支、发布提交或提交范围、用户可感知功能、技术与流程变化、影响范围、配置或数据库变化、验证结果、发布结果及回滚说明。
- 推送 `prd` 前先创建版本文档并填入待发布内容；推送后补充最终 `prd` 提交、自动发布结果和线上验证结果。
- 尚未创建版本文档，或文档仍缺少本次发布的必要信息时，不得推送 `prd`，也不得将发布任务标记为完成。
